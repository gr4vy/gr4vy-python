# Paymentcardnetwork

Card network.

## Example Usage

```python
from gr4vy.models import Paymentcardnetwork

# Open enum: unrecognized values are captured as UnrecognizedStr
value: Paymentcardnetwork = "VISA"
```


## Values

This is an open enum. Unrecognized values will not fail type checks.

- `"VISA"`
- `"MASTERCARD"`
- `"DISCOVER"`
