# Paymentcardtype

Card type.

## Example Usage

```python
from gr4vy.models import Paymentcardtype

# Open enum: unrecognized values are captured as UnrecognizedStr
value: Paymentcardtype = "CREDIT"
```


## Values

This is an open enum. Unrecognized values will not fail type checks.

- `"CREDIT"`
- `"DEBIT"`
