# CallbackEvent

## Example Usage

```python
from gr4vy.models import CallbackEvent

# Open enum: unrecognized values are captured as UnrecognizedStr
value: CallbackEvent = "SHIPPING_ADDRESS"
```


## Values

This is an open enum. Unrecognized values will not fail type checks.

- `"SHIPPING_ADDRESS"`
- `"SHIPPING_OPTIONS"`
