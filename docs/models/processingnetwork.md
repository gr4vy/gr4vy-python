# ProcessingNetwork

## Example Usage

```python
from gr4vy.models import ProcessingNetwork

# Open enum: unrecognized values are captured as UnrecognizedStr
value: ProcessingNetwork = "VISA"
```


## Values

This is an open enum. Unrecognized values will not fail type checks.

- `"VISA"`
- `"MASTERCARD"`
- `"DISCOVER"`
