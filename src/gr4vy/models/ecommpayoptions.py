"""Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT."""

from __future__ import annotations
from gr4vy.types import BaseModel, Nullable, OptionalNullable, UNSET, UNSET_SENTINEL
from pydantic import model_serializer
from typing_extensions import NotRequired, TypedDict


class EcommpayOptionsTypedDict(TypedDict):
    booking_start_date: NotRequired[Nullable[str]]
    r"""The start date of the booking in ISO 8601 format (YYYY-MM-DD). Required for certain MCCs."""
    booking_end_date: NotRequired[Nullable[str]]
    r"""The end date of the booking in ISO 8601 format (YYYY-MM-DD). Required for certain MCCs."""


class EcommpayOptions(BaseModel):
    booking_start_date: OptionalNullable[str] = UNSET
    r"""The start date of the booking in ISO 8601 format (YYYY-MM-DD). Required for certain MCCs."""

    booking_end_date: OptionalNullable[str] = UNSET
    r"""The end date of the booking in ISO 8601 format (YYYY-MM-DD). Required for certain MCCs."""

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(["booking_start_date", "booking_end_date"])
        nullable_fields = set(["booking_start_date", "booking_end_date"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))
            is_nullable_and_explicitly_set = (
                k in nullable_fields
                and (self.__pydantic_fields_set__.intersection({n}))  # pylint: disable=no-member
            )

            if val != UNSET_SENTINEL:
                if (
                    val is not None
                    or k not in optional_fields
                    or is_nullable_and_explicitly_set
                ):
                    m[k] = val

        return m
