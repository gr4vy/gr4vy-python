"""Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT."""

from __future__ import annotations
from gr4vy.types import BaseModel, Nullable, OptionalNullable, UNSET, UNSET_SENTINEL
from pydantic import model_serializer
from typing_extensions import NotRequired, TypedDict


class GiftCardIssuanceCreateTypedDict(TypedDict):
    r"""The details used to issue a new virtual gift card."""

    theme: str
    r"""The provider theme code to issue the gift card against."""
    amount: int
    r"""The amount to load onto the gift card, in the smallest denomination for the currency."""
    currency: str
    r"""The ISO-4217 currency code for the `amount`."""
    external_identifier: NotRequired[Nullable[str]]
    r"""An optional external identifier for this issuance."""


class GiftCardIssuanceCreate(BaseModel):
    r"""The details used to issue a new virtual gift card."""

    theme: str
    r"""The provider theme code to issue the gift card against."""

    amount: int
    r"""The amount to load onto the gift card, in the smallest denomination for the currency."""

    currency: str
    r"""The ISO-4217 currency code for the `amount`."""

    external_identifier: OptionalNullable[str] = UNSET
    r"""An optional external identifier for this issuance."""

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(["external_identifier"])
        nullable_fields = set(["external_identifier"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))
            is_nullable_and_explicitly_set = (
                k in nullable_fields
                and (self.__pydantic_fields_set__.intersection({n}))  # pylint: disable=no-member
            )

            if val != UNSET_SENTINEL:
                if (
                    val is not None
                    or k not in optional_fields
                    or is_nullable_and_explicitly_set
                ):
                    m[k] = val

        return m
