"""Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT."""

from __future__ import annotations
from gr4vy.types import (
    BaseModel,
    Nullable,
    OptionalNullable,
    UNSET,
    UNSET_SENTINEL,
    UnrecognizedStr,
)
from pydantic import model_serializer
from typing import Literal, Optional, Union
from typing_extensions import NotRequired, TypedDict


Source = Union[
    Literal[
        "web",
        "mobile",
    ],
    UnrecognizedStr,
]
r"""The platform that the Paze session is being created for. Defaults to `web`."""


class PazeSessionRequestTypedDict(TypedDict):
    source: NotRequired[Source]
    r"""The platform that the Paze session is being created for. Defaults to `web`."""
    domain_name: NotRequired[Nullable[str]]
    r"""The domain on which Paze is being loaded. Required when `source` is `web`."""


class PazeSessionRequest(BaseModel):
    source: Optional[Source] = "web"
    r"""The platform that the Paze session is being created for. Defaults to `web`."""

    domain_name: OptionalNullable[str] = UNSET
    r"""The domain on which Paze is being loaded. Required when `source` is `web`."""

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(["source", "domain_name"])
        nullable_fields = set(["domain_name"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))
            is_nullable_and_explicitly_set = (
                k in nullable_fields
                and (self.__pydantic_fields_set__.intersection({n}))  # pylint: disable=no-member
            )

            if val != UNSET_SENTINEL:
                if (
                    val is not None
                    or k not in optional_fields
                    or is_nullable_and_explicitly_set
                ):
                    m[k] = val

        return m
